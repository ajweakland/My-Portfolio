export default function WorkHistory() {
    const [activeTab, setActiveTab] = useState('work'); // State to track which tab is active

    const handleTabClick = (tab) => {
        setActiveTab(tab);
    };

    return (
        <p></p>
    );
}